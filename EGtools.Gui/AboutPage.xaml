<Page
    x:Class="EGtools.Gui.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EGtools.Gui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

  <ScrollViewer>
    <StackPanel Spacing="18" Padding="28,24" MaxWidth="720" HorizontalAlignment="Left">
      <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
        <Image x:Name="AboutLogoImg" Width="72" Height="72" />
        <StackPanel VerticalAlignment="Center">
          <TextBlock Text="EGtools" FontSize="28" FontWeight="Bold" />
          <TextBlock x:Name="VerText" FontSize="13" Foreground="Gray" />
        </StackPanel>
      </StackPanel>

      <TextBlock x:Name="Desc" TextWrapping="Wrap" />

      <Border Background="{ThemeResource CardBg}"
              BorderBrush="{ThemeResource CardBorder}" BorderThickness="1" CornerRadius="10" Padding="14">
        <StackPanel Spacing="8">
          <TextBlock x:Name="FeaturesLabel" FontWeight="SemiBold" />
          <TextBlock x:Name="Feat1" />
          <TextBlock x:Name="Feat2" />
          <TextBlock x:Name="Feat3" />
          <TextBlock x:Name="Feat4" />
        </StackPanel>
      </Border>

      <Border Background="{ThemeResource CardBg}"
              BorderBrush="{ThemeResource CardBorder}" BorderThickness="1" CornerRadius="10" Padding="14">
        <StackPanel Spacing="6">
          <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock x:Name="AuthorLabel" FontWeight="SemiBold" Width="80" />
            <TextBlock Text="HerryABU" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock x:Name="VersionLabel" FontWeight="SemiBold" Width="80" />
            <TextBlock x:Name="VersionValue" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock x:Name="LicenseLabel" FontWeight="SemiBold" Width="80" />
            <TextBlock Text="MIT" />
          </StackPanel>
        </StackPanel>
      </Border>

      <Button x:Name="DocsBtn" Click="DocsBtn_Click" />
    </StackPanel>
  </ScrollViewer>
</Page>
